# sparse-embedder — 격리 환경 (기존 embedder/.venv 와 완전 분리)
#
# 기존 embedder 는 transformers<4.50 + SentenceTransformer 로 dense 만 처리.
# 여기는 FlagEmbedding 1.4.0 의 sparse(lexical_weights) 가 필요해서
# transformers>=4.56 (from_pretrained(dtype=) 지원) 으로 별도 정합한다.
# 두 환경은 venv 가 달라 서로 영향 없음.  자세한 배경: docs/hybrid-search-progress.md

# pyarrow 를 다른 무거운 네이티브 import 보다 먼저 로드해야 segfault 회피
# (server.py / backfill_sparse.py 최상단에서 import pyarrow 한다)
pyarrow

FlagEmbedding==1.4.0
transformers>=4.56
torch

psycopg[binary]==3.2.3
pgvector==0.3.6

fastapi==0.115.5
uvicorn==0.32.1
pydantic==2.10.3
python-dotenv==1.0.1
